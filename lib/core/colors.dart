import 'package:flutter/material.dart';

const badgecolor = Color.fromARGB(255, 221, 21, 98);
const bottomNavigationbarcolor = Color.fromARGB(255, 0, 38, 88);

//bottom bar icon
const unselectedIconColor = Colors.amber;
const selectedIconColor = Colors.white;

//condition wise selected icon in bottombar
const iconSelectedContainerColor = Colors.white10;
const iconUnSelectedContainerColor = Colors.transparent;

//loading indicator
const circularprogressindicatorcolors = Colors.amber;
